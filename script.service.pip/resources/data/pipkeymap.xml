<keymap>
  <global>
    <keyboard>
      <p mod="longpress">NotifyAll(service.pip, toggle_pip)</p>
      <p mod="shift,ctrl">NotifyAll(service.pip, toggle_pip)</p>
      <b mod="shift,ctrl">NotifyAll(service.pip, channel_back_pip)</b>
      <up mod="shift,ctrl">NotifyAll(service.pip, channel_up_pip)</up>
      <down mod="shift,ctrl">NotifyAll(service.pip, channel_down_pip)</down>
    </keyboard>
  </global>
</keymap>
